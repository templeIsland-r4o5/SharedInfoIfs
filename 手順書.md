# 手順書: Shared Info 後継機能 カスタム画面作成ガイド

## 1. 概要

本ドキュメントは、「Shared Info」システムの主要機能である「申し送り事項（Turn Note）」および「注視事象（Ship Monitoring）」を、IFS Cloud Aurenaクライアントの標準Configuration機能（ノーコード／ローコード）のみを使用して構築するための詳細手順を解説します。

### 1.1. 完成形イメージ

- **申し送り事項**: `Aircraft Turn Details` 画面内に、Turnに紐づく一時的な申し送り事項を記録・共有する機能を追加する。
- **注視事象**: 予防整備や経過観察が必要な事象を管理するための、独立したカスタムページ「Ship Monitoring」を作成する。このページでは、事象の登録、ステータス管理、および関連タスクとの連携が可能。

### 1.2. 採用するConfiguration機能

- **Custom Entity**: 申し送り事項、注視事象、および関連タスク連携用の新しいデータ格納領域（テーブル）を作成します。
- **Custom Page (Page Designer)**: 「Ship Monitoring」用の新規ページを作成します。
- **Page Configuration**: 既存の `Aircraft Turn Details` 画面にセクションとリストを追加します。
- **Custom Commands**: 画面にカスタムアクション（ボタン）を追加します。
- **IFS Cloud BPA (Workflow)**: 関連タスクの完了をトリガーに、注視事象のステータスを自動更新します。
- **Custom Events & Event Actions**: BPAワークフローを起動するためのイベントを定義します。
- **Navigator Designer**: 作成した画面をナビゲーションメニューに登録します。

---

## 2. Part 1: データモデルの設計と作成 (Custom Entities)

`SharedInfo.md`の要件に基づき、3つのカスタムエンティティを作成します。

### 2.1. Custom Entityの作成手順（共通）

このセクションでは、カスタムエンティティを作成する際の共通手順を、クリック操作のレベルで詳細に解説します。

1.  **Entity Configurations 画面へのナビゲーション**:
    *   画面左上のハンバーガーメニュー（ナビゲーター）をクリックします。
    *   `Solution Manager` > `Configuration` と展開し、`Entity Configurations` をクリックします。

2.  **新規エンティティの作成開始**:
    *   `Entity Configurations` 画面のリストビュー上部にあるツールバーの `+` アイコンをクリックします。
    *   `General` セクションに新しいレコード行が追加され、編集可能状態になります。

3.  **エンティティ基本情報の入力**:
    *   追加された行で、各エンティティの「エンティティ定義」に従って、以下の項目を正確に入力します。
        *   **Entity**: エンティティの技術的な名称を入力します（例: `CustomTurnNote`）。**注意:** このIDは一度保存すると変更できません。
        *   **Prompt**: 画面に表示される際のラベルを入力します（例: `Custom Turn Note`）。
        *   **Module**: プルダウンから `CONFIG` を選択します。
    *   **`Create Projection` チェックボックスはチェックしない**でください。API（Projection）は後続のステップで手動で作成・拡張します。
    *   **タイムゾーン設定の確認**:
        *   エンティティに日付や時刻の属性（`Date & Time`型）が含まれる場合、`Time Zone Default Setting` の設定を検討します。
        *   `None` (デフォルト): タイムゾーン変換をサポートしません。
        *   `Server` / `Site`: サーバーやサイトのタイムゾーン設定で日付/時刻を扱います。今回の要件では`Server`を選択することが推奨されます。
    *   入力が終わったら、ツールバーの `Save` をクリックしてエンティティのヘッダー情報を保存します。

4.  **カスタム属性 (Attributes) の定義**:
    *   作成したエンティティ（例: `CustomTurnNote`）がリストで選択されていることを確認します。
    *   画面下半分に表示される `Custom Attributes` セクションのツールバーにある `+` アイコンをクリックします。
    *   `Add Custom Attribute` アシスタント（ウィザード）が表示されます。
    *   **ステップ 1: Custom Attribute Type**: `Persistent Field` を選択し、「Next」をクリックします。
    *   **ステップ 2: Properties**:
        *   「属性定義一覧」の各行について、以下の情報を入力します。
            *   **Attribute Name**: 属性の技術名称（例: `TurnNoteId`）。
            *   **Prompt**: 画面表示用のラベル（例: `Turn Note ID`）。
            *   **Data Type**: ドロップダウンから適切なデータ型（`Number`, `String`, `Date & Time`など）を選択します。`String`の場合は`Length`も指定します。
        *   入力後、「Next」をクリックします。
    *   **ステップ 3: Approve Information**:
        *   **`Approve` チェックボックスをオンにします。**
        *   `Finish` をクリックしてアシスタントを完了します。
    *   上記手順を、定義するすべての属性について繰り返します。

5.  **エンティティ全体の承認と公開 (Approve & Publish)**:
    *   すべての属性を定義し終えたら、エンティティをシステム全体で利用可能にするための最終処理を行います。
    *   **エンティティの承認**: 画面上部のコマンドバーにある `Approve` ボタンをクリックします。エンティティの `Approved` ステータスが `Yes` に変わります。
    *   **エンティティの公開**: 続けて、コマンドバーにある `Publish` ボタンをクリックします。これにより、データベースに物理的なテーブルが作成され、`Published` ステータスが `Yes` に変わります。この処理には数秒から数十秒かかる場合があります。

> **✅ Point: 承認と公開の重要性**
> - **Approve**: このエンティティと属性の定義が「正」であることを承認する行為です。
> - **Publish**: 承認された定義を基に、実際のデータベースオブジェクトを作成し、IFSシステム内で利用可能な状態にする行為です。両方の操作を完了しないと、後続のProjectionや画面作成で使用できません。

> **📚 参考資料**:
> - `Custom Entities - Technical Documentation For IFS Cloud.pdf`
> - `Custom Attributes - Technical Documentation For IFS Cloud.pdf`

---

### 2.2. エンティティ1: 申し送り事項 (Turn Note)

#### エンティティ定義

| プロパティ | 値 |
|---|---|
| Entity | `CustomTurnNote` |
| Prompt | `Custom Turn Note` |
| Module | `CONFIG` |

#### 属性定義一覧

| 項目名 (Attribute Name) | 表示ラベル (Prompt) | データ型 (Data Type) | 長さ (Length) |
|---|---|---|---|
| `TurnNoteId` | Turn Note ID | `Number` | |
| `TurnId` | Turn ID | `Number` | |
| `NoteText` | 申し送り内容 | `String` | `2000` |
| `ReportedBy` | 登録者 | `String` | `30` |
| `ReportedAt` | 登録日時 | `Date & Time` | |
| `Role` | 登録者ロール | `String` | `50` |

*補足: `TurnNoteId` を主キー、`TurnId` を `AircraftTurn` への外部キーとして後ほどProjectionで関連付けます。*

---

### 2.3. エンティティ2: 注視事象 (Ship Monitoring)

#### エンティティ定義

| プロパティ | 値 |
|---|---|
| Entity | `CustomShipMonitoring` |
| Prompt | `Custom Ship Monitoring` |
| Module | `CONFIG` |

#### 属性定義一覧

| 項目名 (Attribute Name) | 表示ラベル (Prompt) | データ型 (Data Type) | 長さ (Length) | デフォルト値ロジック |
|---|---|---|---|---|
| `MonitoringId` | モニタリングID | `Number` | | Sequenceで自動採番 |
| `ShipNo` | 機番 | `String` | `10` | |
| `Title` | 件名 | `String` | `200` | |
| `Description` | 詳細 | `String` | `4000` | |
| `Status` | 状態 | `String` | `30` | `'OPEN'` |
| `ReportedBy` | 起票者 | `String` | `30` | |
| `CreatedAt` | 起票日時 | `Date & Time` | | `SYSTIMESTAMP` |
| `ClosedBy` | クローズ者 | `String` | `30` | |
| `ClosedAt` | クローズ日時 | `Date & Time` | | |

---

### 2.4. エンティティ3: タスク連携 (Task Link)

#### エンティティ定義

| プロパティ | 値 |
|---|---|
| Entity | `MonitoringTaskLink` |
| Prompt | `Monitoring Task Link` |
| Module | `CONFIG` |

#### 属性定義一覧

| 項目名 (Attribute Name) | 表示ラベル (Prompt) | データ型 (Data Type) | 長さ (Length) | 備考 |
|---|---|---|---|---|
| `LinkSeq` | Link Seq | `Number` | | 主キー (自動採番) |
| `MonitoringId` | モニタリングID | `Number` | | `CustomShipMonitoring`へのFK |
| `TaskObjkey` | Task Objkey | `String` | `50` | `JtTask.Objkey` へのFK |

---

## 3. Part 2: APIの作成 (Custom Projection)

作成したカスタムエンティティを画面から利用するためのAPI（Projection）を作成します。これはIFS Developer Studio等の開発ツールを使用する作業です。

1.  **Projectionファイルの作成**:
    *   `CustomShipMonitoringHandling.projection` というファイル名で新規作成します。
2.  **エンティティの公開 (basedon, entityset)**:
    *   `CustomShipMonitoring`, `CustomTurnNote`, `MonitoringTaskLink` を `basedon` で定義し、それぞれの `entityset` を作成します。
3.  **関連 (Associations) の定義**:
    *   `CustomTurnNote` と `AircraftTurn` を `TurnId` で関連付けます。
    *   `CustomShipMonitoring` と `MonitoringTaskLink` を `MonitoringId` で関連付けます。
    *   `MonitoringTaskLink` と `JtTask` を `TaskObjkey` と `Objkey` で関連付けます。
4.  **アクション (Actions) と関数 (Functions) の定義**:
    *   ステータスを更新するためのアクション `UpdateStatus` などを定義します。
5.  **モデルのデプロイ**:
    *   作成したProjectionファイルを環境にデプロイします。

---

## 4. Part 3: 画面の作成と変更 (Page Designer & Page Configuration)

### 4.1. `Aircraft Turn Details` 画面への申し送り機能追加

既存の `Aircraft Turn Details` 画面に、選択したTurnに紐づく「申し送り事項」を一覧表示するセクションを追加します。これはマスター/ディテールの関係を画面上で表現する典型的な例です。

1.  **Page Designerの起動**:
    *   `Aircraft Turn Details` 画面を開きます。
    *   画面右側のツールパネルにある盾のアイコン（Page Designer）をクリックして、設定モードに入ります。

2.  **`All Elements` へのリストコンポーネント定義**:
    *   Page Designerの左ペインは、上部が `Active Page`（現在のページレイアウト）、下部が `All Elements`（ページで利用可能な部品置き場）に分かれています。
    *   まず、`All Elements` に新しいリストを追加します。`All Elements` の `+` をクリックし、`List` を選択します。
    *   作成されたリストの設定を以下のように行います。
        *   **Name**: `TurnNoteList` （識別しやすい名前）
        *   **Label**: `申し送り事項 (Turn Notes)`
        *   **Entity**: `CustomTurnNote` （Projectionで公開したエンティティセット名）
    *   `TurnNoteList` の `Content` を展開し、`+` をクリックしてリストに表示したいフィールドを追加します。
        *   `NoteText`, `ReportedBy`, `ReportedAt` の3つの `Data Field` を追加します。それぞれの `Source` が対応する属性名になっていることを確認してください。
    *   `TurnNoteList` の直下に `Crud Actions` を追加し、`create`, `read`, `update`, `delete` が有効になっていることを確認します。これにより、リストからの新規作成や編集が可能になります。

3.  **`Active Page` へのリスト配置とデータ連携**:
    *   次に、定義したリストを実際のページレイアウトに配置します。
    *   `Active Page` の `Content` > `...` > `Content` と展開し、レイアウトの末尾にある `+` をクリックして `List` を選択します。
    *   追加された `List` のプロパティで、`reference` に先ほど作成した `TurnNoteList` を指定します。
    *   **（重要）データ連携設定**: このリストが親である `Aircraft Turn` のレコードと連動するように `Binding` を設定します。
        *   **注意**: Binding設定中にエラーが発生することを防ぐため、一時的に画面上部のプレビュー（`Preview`）スライダーをオフにすることをお勧めします。
        *   追加したリスト `TurnNoteList` の下にある `binding` をクリックします。
        *   以下の通りプロパティを設定します。
            *   **bindname**: `AircraftTurnSelector` （親となる `Aircraft Turn` のメインセレクター名）
            *   **property**: `CustomTurnNoteArray` （Part 2のProjectionで定義した `AircraftTurn` から `CustomTurnNote` へのAssociation名）

4.  **保存と公開**:
    *   すべての設定が完了したら、`Preview` を再度オンにして意図した通りに表示されるか確認します。
    *   画面上部の `Save` ボタンでここまでの設定を保存します。
    *   最後に `Publish` ボタンをクリックして、変更をすべてのユーザーに公開します。

> **✅ Point: Page Designerの基本コンセプト**
> - **`All Elements`**: そのページで利用できる、再利用可能なUIコンポーネント（リスト、グループ、フィールド等）を事前に定義しておく場所です。
> - **`Active Page`**: `All Elements` で定義したコンポーネントへの参照を配置し、ページの実際の構造（どこに何を表示するか）を組み立てる場所です。
> - **`Binding`**: 親要素（マスター）と子要素（ディテール）を関連付け、親の選択内容に応じて子の表示内容が動的に変わるようにするための重要な設定です。

> **📚 参考資料**:
> - `Building Master Detail Associations - Technical Documentation For IFS Cloud.pdf`
> - `UI - Page Designer - Technical Documentation For IFS Cloud.pdf`

### 4.2. 「Ship Monitoring」カスタムページの作成

`SharedInfo.md`の要件に基づき、注視事象を管理するための独立した新しいページを作成します。

1.  **新規ページの作成開始**:
    *   ナビゲーターから `Solution Manager` > `Configuration` > `Pages` に移動します。
    *   `+ New Page` ボタンをクリックします。
    *   `Create New Page` アシスタントが表示されます。
        *   **Page Name**: `CustomShipMonitoring`
        *   **Projection**: `CustomShipMonitoringHandling`
        *   **Page Title**: `Ship Monitoring`
    *   `OK` をクリックすると、Page Designerが開き、新しいページの雛形が作成されます。

2.  **`All Elements` への主要コンポーネント定義**:
    *   まず、このページで使用するUI部品を `All Elements` にすべて定義します。
    *   **a. メインセレクターの定義**:
        *   `All Elements` の `+` から `Selector` を選択します。
        *   **Name**: `ShipMonitoringSelector`
        *   **Entity**: `CustomShipMonitoring`
        *   `Content` に、セレクターのドロップダウンに表示したいフィールド（例: `MonitoringId`, `Title`）を追加します。
    *   **b. メインリストの定義**:
        *   `All Elements` の `+` から `List` を選択します。
        *   **Name**: `ShipMonitoringList`
        *   **Entity**: `CustomShipMonitoring`
        *   `Content` に、一覧表示したいフィールド（`MonitoringId`, `ShipNo`, `Title`, `Status`, `CreatedAt`）を追加します。
    *   **c. 詳細情報グループの定義**:
        *   `All Elements` の `+` から `Group` を選択します。
        *   **Name**: `ShipMonitoringDetailGroup`
        *   **Entity**: `CustomShipMonitoring`
        *   `Content` に、詳細表示したいすべてのフィールド（`Description`など）を追加します。
    *   **d. 関連タスクリストの定義**:
        *   `All Elements` の `+` から `List` を選択します。
        *   **Name**: `MonitoringTaskLinkList`
        *   **Entity**: `JtTask` （関連先のエンティティ）
        *   `Content` に、タスク情報（`Barcode`, `Description`, `Objstate`）を追加します。

3.  **`Active Page` でのレイアウト構築とデータ連携**:
    *   次に、`Active Page` を編集してページの骨格を作ります。
    *   **a. セレクターの配置**:
        *   `Active Page` の `content` の `+` をクリックし、`Selector` を選択します。
        *   `reference` プロパティに、先ほど作成した `ShipMonitoringSelector` を指定します。
    *   **b. メインリストの配置**:
        *   `Active Page` の `content` の `+` から `List` を選択し、`reference` に `ShipMonitoringList` を指定します。
        *   **Binding設定**: このリストがメインセレクターと連動するように設定します。
            *   `binding` をクリックし、`bindname` に `ShipMonitoringSelector` を指定します。（propertyは空）
    *   **c. タブコンテナとタブの作成**:
        *   `Active Page` の `content` の `+` から `Tabs` を追加します。
        *   作成した `Tabs` の `content` の `+` から `Tab` を追加し、`label` を「詳細情報」とします。
        *   同様にもう一つ `Tab` を追加し、`label` を「関連タスク」とします。
    *   **d. 「詳細情報」タブへのグループ配置と連携**:
        *   「詳細情報」タブの `content` の `+` から `Group` を選択し、`reference` に `ShipMonitoringDetailGroup` を指定します。
        *   **Binding設定**: `binding` をクリックし、`bindname` に `ShipMonitoringSelector` を指定します。
    *   **e. 「関連タスク」タブへのリスト配置と連携**:
        *   「関連タスク」タブの `content` の `+` から `List` を選択し、`reference` に `MonitoringTaskLinkList` を指定します。
        *   **Binding設定**:
            *   `bindname`: `ShipMonitoringSelector`
            *   `property`: `MonitoringTaskLinkArray` （Projectionで定義した`CustomShipMonitoring`から`MonitoringTaskLink`へのAssociation名）

4.  **カスタムコマンドの追加**:
    *   **a. 新規作成コマンド**:
        *   `ShipMonitoringList` の `content` の `+` から `Command` を選択します。
        *   **Command Type**: `Crud` > `Create`
        *   **Label**: `注視事象の新規作成`
    *   **b. 関連タスク追加コマンド**:
        *   `MonitoringTaskLinkList` の `content` の `+` から `Command` を選択します。
        *   **Command Type**: `Navigate` > `assistant`
        *   **Label**: `関連タスクの追加`
        *   **assistant**: `JtTaskSelector` などを指定し、タスクを選択できるアシスタントを呼び出すように設定します。

5.  **保存と公開**:
    *   `Save` ボタンで設定を保存し、`Publish` ボタンでページを公開します。

> **📚 参考資料**:
> - `Creating a New Page Model (Custom Page) - Technical Documentation For IFS Cloud.pdf`
> - `Building Master Detail Associations - Technical Documentation For IFS Cloud.pdf`

---

## 5. Part 4: ビジネスプロセス自動化 (BPA & Events)

### 5.1. Custom Eventの作成

関連タスクのステータスが「完了」になったことを検知し、後続のBPA（Workflow）を起動するためのイベントトリガーを作成します。

1.  **Events画面へのナビゲーション**:
    *   ナビゲーターから `Solution Manager` > `Configuration` > `Events` に移動します。

2.  **新規イベントの作成**:
    *   `Events` 画面のツールバーにある `+` をクリックして、新しいイベント定義を作成します。
    *   以下の通り、各項目を入力・設定します。
        *   **Event ID**: `EV_JT_TASK_COMPLETED` （一意で分かりやすいID）
        *   **Description**: `タスク完了時に注視事象のステータスを更新するためのイベント` （イベントの目的を記述）
        *   **Logical Unit**: `JtTask` （イベントを監視する対象のLU）
        *   **Event Type**: `Model Event` を選択します。これはIFSの標準データモデルの変更をトリガーとすることを意味します。
        *   **Table Name**: `JT_TASK_TAB` （Logical Unitを選択すると自動的に入力されます）

3.  **トリガータイミングと条件の設定**:
    *   `Fires When` セクションで、イベントが発火するタイミングを定義します。
        *   `On Update` チェックボックスをオンにします。
    *   `Event Condition` セクションに、より詳細な発火条件をPL/SQLライクな式で記述します。
        *   `NEW:Objstate = 'Finished' AND OLD:Objstate != 'Finished'`
        *   （`Objstate`カラムの値が 'Finished' 以外から 'Finished' に変更された瞬間のみ発火させる、という条件式です）

4.  **イベントアクションの定義**:
    *   画面下部の `Event Actions` セクションのツールバーにある `+` をクリックします。
    *   `Action Type` ドロップダウンから `Execute Workflow` を選択します。
    *   新しく表示された `Workflow ID` フィールドに、このイベントによって起動したいBPAのIDを入力します。
        *   **Workflow ID**: `wf_UpdateMonitoringStatus` （後ほど作成するBPAのID）
    *   `Enabled` チェックボックスがオンになっていることを確認します。

5.  **イベントの有効化**:
    *   すべての設定が完了したら、画面上部のコマンドバーにある `Activate` ボタンをクリックします。
    *   **✅ Point**: この「有効化」操作を行わないと、イベントは定義されているだけで、実際のデータ変更には全く反応しません。非常に重要なステップです。

---

*この後、Part 5.2 BPA (Workflow) の作成が続きます。*

### 5.2. BPA (Workflow) の作成

前のステップで作成したイベントをトリガーに、実際のビジネスロジック（ステータス更新）を実行するワークフローを、BPA Designerを使って設計します。

1.  **BPA Designerへのナビゲーション**:
    *   ナビゲーターから `Solution Manager` > `Business Process Automation` > `BPA Designer` に移動します。

2.  **新規ワークフローの作成**:
    *   `BPA Designer` が開いたら、`Create` ボタンをクリックして新しいワークフローの設計を開始します。
    *   **ID**: `wf_UpdateMonitoringStatus`
    *   **Title**: `注視事象ステータス自動更新`
    *   設計キャンバスが表示されます。

3.  **開始イベント（Start Event）の設定**:
    *   キャンバス上の丸い「Start」アイコンをクリックします。
    *   右側のプロパティパネルで、トリガーを設定します。
        *   **Trigger Type**: `Event Invoked`
        *   **Event Id**: `EV_JT_TASK_COMPLETED` を選択します。
    *   **✅ Point**: これにより、このBPAは`EV_JT_TASK_COMPLETED`イベントが発生したときに自動的に開始されるようになります。イベントから渡されたキー情報（この場合 `JtTask` の `Objkey`）は、`task_objkey` という名前のワークフロー変数として自動的に格納されます。

4.  **プロセスフローの設計 (BPMN)**:
    *   キャンバスの左側にあるツールパレットから、BPMNの要素をドラッグ＆ドロップしてフローを組み立てます。
    *   **a. Service Task: 関連チェック**:
        *   パレットから `Service Task`（歯車のアイコン）を開始イベントの右側にドラッグします。
        *   **Name**: `関連モニタリングIDの取得`
        *   **Task Type**: `Call Projection`
        *   **Projection Name**: `CustomShipMonitoringHandling`
        *   **Operation Type**: `Query` (または `Function`)
        *   **Operation**: `MonitoringTaskLinks` EntitySetを選択し、フィルター条件を設定します。
            *   `$filter=TaskObjkey eq '${task_objkey}'`
        *   **Output Mapping**: クエリ結果から `MonitoringId` を取り出し、新しいワークフロー変数（例: `monitoring_id`）にマッピングします。
    *   **b. Exclusive Gateway: 分岐**:
        *   パレットから `Exclusive Gateway`（ひし形にXのアイコン）をドラッグします。
        *   このゲートウェイから2本の線（シーケンスフロー）を引きます。
            *   **上方向のフロー**: `モニタリングID有り`。条件式に `${monitoring_id != null}` を設定します。
            *   **下方向のフロー**: `モニタリングID無し`。これをDefault Flowに設定します。
    *   **c. Service Task: ステータス更新**:
        *   `モニタリングID有り` のフローの先に、新しい `Service Task` をドラッグします。
        *   **Name**: `ステータスをIN_PROGRESSに更新`
        *   **Task Type**: `Call Projection`
        *   **Projection Name**: `CustomShipMonitoringHandling`
        *   **Operation Type**: `Action`
        *   **Action Name**: `UpdateStatus` （Part 2で定義したアクション）
        *   **Input Mapping**: アクションに必要なパラメータをワークフロー変数からマッピングします。
            *   `MonitoringId`: `${monitoring_id}`
            *   `NewStatus`: `'IN_PROGRESS'` （固定値）
    *   **d. End Event: 終了**:
        *   すべてのフローの終点に、パレットから `End Event`（太線の丸いアイコン）をドラッグして接続します。

5.  **デプロイと有効化**:
    *   フローの設計が完了したら、画面右上の `Save` ボタンで保存します。
    *   続いて `Deploy` ボタンをクリックします。バージョン番号などを確認するダイアログが表示されるので、`OK` をクリックしてワークフローをサーバーに展開します。
    *   **✅ Point**: デプロイが成功すると、ワークフローは自動的に有効化（Active）され、イベントを受信待機状態になります。

---

## 6. Part 5: ナビゲーションと権限

### 6.1. ナビゲーションメニューへの登録

作成した「Ship Monitoring」カスタムページを、ユーザーがアクセスできるようにナビゲーションメニューに追加します。

1.  **Navigator Designerの起動**:
    *   画面右上のユーザーアイコンをクリックし、表示されるメニューから `Navigator Designer` を有効にします。
    *   画面左側のナビゲーションエリアがNavigator Designerモードに切り替わります。

2.  **メニュー項目の追加**:
    *   作成したページを追加したいメニュー階層を探します（例: `Hangar Maintenance`）。
    *   追加したい親メニュー項目にマウスをホバーすると表示される `+` アイコンをクリックします。

3.  **新規メニュー項目のプロパティ設定**:
    *   `Create Entry` ダイアログが表示されるので、以下の通り設定します。
        *   **Entry Type**: `Page`
        *   **Label**: `Ship Monitoring` （メニューに表示されるテキスト）
        *   **Client**: `CustomShipMonitoringHandling` （ページの基になっているProjection名）
        *   **Page**: `page/CustomShipMonitoring/Form` （ページのURLパス。Page Designerで確認できます）
    *   `OK` をクリックしてダイアログを閉じます。

4.  **保存と公開**:
    *   Navigator Designerの上部にある `Save` ボタンをクリックして、変更内容をコンフィギュレーションとして保存します。
    *   続いて `Publish` ボタンをクリックし、変更をすべてのユーザーに反映させます。

> **📚 参考資料**:
> - `UI - Navigator Designer - Technical Documentation For IFS Cloud.pdf`

### 6.2. 権限設定

作成したカスタムページとAPI（Projection）を、適切なロールを持つユーザーが利用できるように権限を付与します。

1.  **Permission Set画面へのナビゲーション**:
    *   ナビゲーターから `Solution Manager` > `Users and Permissions` > `Permission Sets` に移動します。
    *   権限を付与したい対象のPermission Set（例: `FLM_TECHNICIAN` や、この機能のための新しい権限セット）を選択します。

2.  **Projectionへのアクセス権付与**:
    *   左側のメニューから `Projections` をクリックします。
    *   `Projections and Lobby Pages` リストの上部にある `+` (Add) をクリックします。
    *   表示されるダイアログで `CustomShipMonitoringHandling` を検索し、チェックを入れて `OK` をクリックします。
    *   リストに追加された `CustomShipMonitoringHandling` の行を選択します。
    *   画面下部の `Actions` セクションで、このロールに許可する操作（例: `UpdateStatus`）のチェックボックスをオンにします。
    *   同様に、`Entity Actions` セクションで、各エンティティに対するCRUD（Create, Read, Update, Delete）権限を適切に設定します。例えば、`CustomTurnNote` に対してはフルアクセスを許可し、`CustomShipMonitoring` はRead-onlyにする、といった制御が可能です。

3.  **カスタムページへの表示権限付与**:
    *   左側のメニューから `Pages` をクリックします。
    *   `Pages` リストの上部にある `+` (Add) をクリックします。
    *   `page/CustomShipMonitoring/Form` を検索し、チェックを入れて `OK` をクリックします。これで、この権限セットを持つロールのユーザーは、ナビゲーションメニューからこのページにアクセスできるようになります。

4.  **変更の保存と反映**:
    *   画面上部の `Save` ボタンで、ここまでの権限設定を保存します。
    *   **✅ Point**: 権限の変更をシステムに即時反映させるには、セキュリティキャッシュの更新が必要です。`Solution Manager` > `Users and Permissions` > `Permission Set` 画面に戻り、`Refresh Cache` または `Security Caches` 関連のコマンドを実行します。

以上で、すべてのConfiguration作業は完了です。
