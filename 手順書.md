# 手順書: Ship Monitoring カスタム画面作成ガイド

## 1. 概要

本ドキュメントは、開発環境に存在するソースファイル (`CustomShipMonitoring.client`, `CustomShipMonitoringHandling.projection`) を基に、IFS Cloud Aurenaクライアントの標準Configuration機能（ノーコード）のみを使用して、同様の「Ship Monitoring」画面を構築する手順を解説します。

### 1.1. 完成形イメージ

- 申し送り事項を一覧表示し、選択したアイテムの詳細や関連情報を表示する。
- 各事項に対して、既存のタスクを関連付けることができる。

### 1.2. 採用するConfiguration機能

-   **Custom Page (Page Designer)**: 新規に画面を作成します。
-   **Custom Commands**: 画面にカスタムアクション（ボタン）を追加します。
-   **Assistants**: 複数ステップのダイアログ（ウィザード）を構成します。
-   **Navigator Designer**: 作成した画面をナビゲーションメニューに登録します。

### 1.3. 前提条件

-   `CustomShipMonitoringHandling.projection` が静的にデプロイされ、システムで利用可能な状態であること。
-   Configurationを実施するユーザーが、必要な権限（`SolutionManager`, `FND_DESIGNER`等）を保持していること。

---

## 2. Part 1: データモデルの設計と作成 (Entity / Projection)

カスタム画面を作成する前に、画面で利用するデータ構造（Entity）とAPI（Projection）を定義します。

### 2.1. Custom Entityの作成（新しいテーブルの定義）

IFS Cloudに標準で存在しない、独自の情報を格納するためのテーブル（Logical Unit）を作成します。

1.  **Entity Configurationsを開く**:
    *   グローバルメニューから `Solution Manager` > `Configuration` > `Entity Configurations` に移動します。
2.  **新規Entityを作成**:
    *   `+` ボタンで新規レコードを作成します。
    *   **Entity**: 新しいエンティティ名を入力します (例: `CustomShipMonitoring`)。
    *   **Prompt**: 表示ラベルを入力します (例: `Custom Ship Monitoring`)。
    *   **Module**: `CONFIG` を選択します。
    *   **Create Projection**: このチェックボックスをオンにすると、このEntityに対する基本的な操作（CRUD）を持つProjectionが自動生成されます。後から複雑なロジックを追加したい場合は、チェックをオフにして手動でProjectionを別途作成します。
3.  **属性 (Attributes) の定義**:
    *   `Custom Attributes` セクションで `+` をクリックし、テーブルに含めるカラムを定義します。
        *   **Attribute Name**: 属性名 (例: `MonitoringId`)。
        *   **Prompt**: 表示ラベル。
        *   **Field Type**: `Persistent Field` (DBに保存される項目) などを選択。
        *   **Data Type**: `String`, `Number`, `Date` などを選択。
4.  **承認と公開 (Approve & Publish)**:
    *   定義が完了したら、`Approve` ボタンで承認し、`Publish` ボタンでシステムに公開します。公開すると、データベースにテーブルが物理的に作成されます。

> **📚 参考資料**:
> - `Custom Entities - Technical Documentation For IFS Cloud.pdf`
> - `Entity Configurations - Technical Documentation For IFS Cloud.pdf`

### 2.2. Custom Projectionの作成（APIの定義）

画面（クライアント）とサーバー間のデータ通信を担うAPI（Projection）を作成します。単純なEntityのProjectionは自動生成できますが、複数のEntityを組み合わせたり、独自のロジック（Action）を追加したりする場合は手動で作成・拡張します。

*(注: 以下の手順は、Projectionを手動でモデルファイルとして作成・編集する開発者向けのアプローチを想定しています。Configuration機能としての詳細なProjection作成手順は、関連ドキュメントに明記されていません。)*

1.  **`.projection` ファイルの作成**:
    *   Developer Studioなどの開発ツールを使用し、対象モジュールの `model` ディレクトリ配下に新しい `.projection` ファイルを作成します (例: `CustomShipMonitoringHandling.projection`)。
2.  **Projectionの定義**:
    *   ファイル内に、`projection` キーワードを用いてAPIを定義します。
    *   `basedon` キーワードで、基礎となるEntityを指定します。
    *   `entityset` を定義し、クライアント側で利用する際のデータセット名を指定します。
    *   必要な属性（`attributes`）、関連（`associations`）、アクション（`actions`）を定義します。
3.  **モデルのデプロイ**:
    *   作成した `.projection` ファイルを含むモデルをIFS Cloud環境にデプロイします。

> **📚 参考資料**:
> - `Creating a New Page Model (Custom Page) - Technical Documentation For IFS Cloud.pdf` (Projectionの利用側)


---

## 3. Part 2: カスタムページの作成 (Page Designer)

まず、画面の器となる「ページ」を作成します。

1.  **Page Designerを開く**:
    -   グローバルメニューから **Page Designer** を起動します。
2.  **新規ページを作成**:
    -   **New Page** をクリックします。
    -   以下の通り設定し、**OK** をクリックします。
        -   **Page Name**: `CustomShipMonitoring`
        -   **Projection**: `CustomShipMonitoringHandling` (リストから選択)
        -   **Page Title**: `Ship Monitoring`
        -   **Entity Set**: `CustomShipMonitoringSet` (Projectionを選択すると自動で候補が表示)
3.  **メインリストの構成**:
    -   画面中央に表示された `List` コンポーネントを選択します。
    -   `Properties` ペインで `Title` を `Monitoring Items` に設定します。
    -   `Fields` セクションで、以下のフィールドを `CustomShipMonitoringSet` からドラッグ＆ドロップまたは追加します。
        -   `MonitoringId`
        -   `ShipNo`
        -   `Title`
        -   `Status`
        -   `CreatedAt`

> **📚 参考資料**:
> - `Creating a New Page Model (Custom Page) - Technical Documentation For IFS Cloud.pdf`
> - `UI - Page Designer - Technical Documentation For IFS Cloud.pdf`

---

## 4. Part 3: ページレイアウトの構成

メインリストでアイテムを選択した際に、その詳細情報を表示するためのグループと子リストを追加します。

1.  **詳細グループの追加**:
    -   `Elements` ペインから `Group` を、ページ上の空きスペースにドラッグ＆ドロップします。
    -   `Properties` ペインで `Title` を `Details` に設定します。
    -   `Fields` セクションで、以下のフィールドを追加します。
        -   `MonitoringId`
        -   `ShipNo`
        -   `Title`
        -   `Description`
        -   `Status`
        -   `ReportedBy`
        -   `CreatedAt`
        -   `ClosedBy`
        -   `ClosedAt`
2.  **子リスト (Turn Notes) の追加**:
    -   `Elements` ペインから `List` をドラッグ＆ドロップします。
    -   `Properties` ペインで以下の通り設定します。
        -   **Title**: `Turn Notes`
        -   **Datasource Type**: `Association`
        -   **Association**: `TurnNotes` (メインリストのデータソースとの関連)
    -   `Fields` セクションで、`CustomTurnNote` エンティティから以下のフィールドを追加します。
        -   `NoteText`
        -   `ReportedBy`
        -   `ReportedAt`
3.  **子リスト (Linked Tasks) の追加**:
    -   同様に、もう一つ `List` を追加し、以下の通り設定します。
        -   **Title**: `Linked Tasks`
        -   **Datasource Type**: `Association`
        -   **Association**: `LinkedTasks`
    -   `Fields` セクションで、`MonitoringTaskLink` との関連 (`TaskAssoc`) を展開し、以下のフィールドを追加します。
        -   `TaskAssoc.Barcode`
        -   `TaskAssoc.Description`
        -   `TaskAssoc.Objstate` (このフィールドは `Badge` として表示設定するとより効果的です)

> **📚 参考資料**:
> - `Building Master Detail Associations - Technical Documentation For IFS Cloud.pdf`
> - `Attribute Configurations on Pages - Technical Documentation For IFS Cloud.pdf`

---

## 5. Part 4: カスタムコマンドとアシスタントの作成

タスクをリンクするためのボタンと、そのボタンから起動されるアシスタントを作成します。

1.  **カスタムコマンドの作成**:
    -   `CustomShipMonitoringList` (メインリスト) を選択します。
    -   `Elements` ペインで `Command` をリストのヘッダーに追加します。
    -   `Properties` ペインで以下の通り設定します。
        -   **Label**: `Link Task`
        -   **Action**: `Assistant`
2.  **アシスタントの構成**:
    -   上記で `Action` を `Assistant` に設定すると、アシスタントデザイナーが開きます。
    -   **基本情報**:
        -   **Title**: `Link Task`
        -   **Input Parameters**: `MonitoringId` をメインエンティティから選択して追加します。
    -   **ステップ1: タスク選択リストの追加**:
        -   `Elements` ペインから `List` をステップ内にドラッグ＆ドロップします。
        -   `Properties` ペインで以下の通り設定します。
            -   **Title**: `Available Tasks`
            -   **Datasource Type**: `Array`
            -   **Array**: `AvExeTaskArray` (Projectionで定義された配列)
        -   `Fields` セクションで、`AvExeTask` エンティティから以下のフィールドを追加します。
            -   `Barcode`
            -   `Description`
            -   `Objstate` (Badgeとして設定)
    -   **完了 (Finish) ボタンの設定**:
        -   アシスタントの `Finish` ボタンを選択します。
        -   `Properties` ペインで、`Enabled Expression` を `[component.AvailableTasksList.SelectionCount > 0]` に設定し、リストでアイテムが選択されたときのみ有効になるようにします。
        -   `Execute Action` で、Projectionの `LinkTask` アクションを選択します。
        -   `Parameters` を設定し、入力の `MonitoringId` と、リストで選択した `TaskSeq` (`component.AvailableTasksList.Selected.TaskSeq`) を渡します。
    -   アシスタントを保存して閉じます。

> **📚 参考資料**:
> - `Custom Action Configuration - Technical Documentation For IFS Cloud.pdf`
> - `Adding Commands - Technical Documentation For IFS Cloud.pdf`

---

## 6. Part 5: ナビゲーションメニューへの登録

作成したページにユーザーがアクセスできるよう、ナビゲーションメニューに追加します。

1.  **Navigator Designerを開く**:
    -   グローバルメニューから **Navigator Designer** を起動します。
2.  **メニューエントリの作成**:
    -   メニュー内の任意の位置（例: `Hangar Maintenance` の下など）を選択し、**Create Entry** をクリックします。
    -   **Label** に `Ship Monitoring` と入力します。
    -   **Type** で `Page` を選択し、作成した `CustomShipMonitoring` ページを検索・選択します。
    -   設定を保存します。

> **📚 参考資料**:
> - `UI - Navigator Designer - Technical Documentation For IFS Cloud.pdf`

---

## 7. Part 6: 権限設定

最後に、このページと関連機能へのアクセスを制御します。

1.  **Permission Setを開く**:
    -   `Solution Manager` > `Users and Permissions` > `Permission Sets` で、対象のロールに紐づくPermission Setを開きます。
2.  **権限の付与**:
    -   `Projections` タブで、`CustomShipMonitoringHandling` へのアクセス権を付与します。必要に応じて、アクション（`LinkTask`など）単位での制御も可能です。
    -   `Pages` タブで、作成した `CustomShipMonitoring` ページへの表示権限を付与します。
3.  権限の変更を保存し、キャッシュをリフレッシュします。

以上で、すべてのConfiguration作業は完了です。対象のロールを持つユーザーで再ログインし、ナビゲーターから「Ship Monitoring」画面にアクセスできることを確認してください。
